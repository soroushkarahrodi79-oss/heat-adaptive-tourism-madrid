# HATI-Madrid — analysis / figure environment (Python 3.14.5)
# Pinned to the versions actually used to produce the locked tables and publication figures.
# The physical modelling runs in a SEPARATE Python 3.12 environment (.venv_solweig) that is
# not captured here; install it with:  pip install solweig==0.1.0b92 pvlib   (Python 3.12)
# See REPRODUCIBILITY.md.

numpy==2.4.6
pandas==2.3.3
geopandas==1.1.3
rasterio==1.5.0
shapely==2.1.2
pyproj==3.7.2
rasterstats==0.21.0
matplotlib==3.10.9
pillow==12.2.0

# Testing
pytest

# --- SOLWEIG environment (Python 3.12, separate venv; documented, not installed here) ---
# solweig==0.1.0b92
# pvlib            # clear-sky (Ineichen–Perez) irradiance estimate for meteorological forcing
